  //  HiLite_draw_pic (object) -> picture  // $1: {type: "",   //      width: 0,      // total height  //      height: 0,     // total width  //      opacity: 0,  //  0 - 100  //      roundX: 0,   //      roundY: 0  //  properties for specific types:   //  solid, frame, halfFrame:  //      fillColor: ""  // solid; frame; halfFrame; grad  //      lineColor: "",  // css  //      stroke: 0,      // pix (real) - default is 1  //  grad:  //      startColor: ,  //      endColor: ,  //      rot: (0|45|90|-45|-90)  // rotation  //  }  // Written by: Kirk as Designer, Created: 02/16/19, 09:02:13  // ------------------  // Purpose: C_OBJECT($1;$obj)C_PICTURE($0;$pic)C_LONGINT($x;$y;$width;$height;$roundedX;$roundedY;$opacity;$rot)C_REAL($stroke)C_TEXT($lineColor;$fillColor;$svg;$rect;$lineSvg;$startColor;$endColor;$frame)C_COLLECTION($line)$obj:=$1$width:=$obj.width$height:=$obj.heightIf ($obj.fillColor=Null)$fillColor:="white"Else $fillColor:=$obj.fillColorEnd if If ($obj.opacity#Null)$fillColor:=$fillColor+":"+OB Get($obj;"opacity";Is text)End if If ($obj.stroke=Null)$stroke:=1Else $stroke:=$obj.strokeEnd if $roundedX:=OB Get($obj;"roundX";Is longint)$roundedY:=OB Get($obj;"roundY";Is longint)  // --------------------------------------------------------$svg:=SVG_New ($width;$height)Case of : ($obj.type="solid")  // draw rectangle and fill. $rect:=SVG_New_rect ($svg;$x;$y;$width;$height;$roundedX;$roundedY;$lineColor;$fillColor): ($obj.type="frame")  // draw rect, fill$rect:=SVG_New_rect ($svg;$x;$y;$width;$height;$roundedX;$roundedY;$lineColor;$fillColor)  // draw inner rect & stroke$lineColor:=$obj.lineColor$frame:=SVG_New_rect ($svg;$x;$y;$width;$height;$roundedX;$roundedY;$lineColor;$fillColor;$stroke): ($obj.type="halfFrame")  // draw rect, fill$rect:=SVG_New_rect ($svg;$x;$y;$width;$height;$roundedX;$roundedY;$lineColor;$fillColor)  // draw line on left & bottom $lineColor:=$obj.lineColor$stroke:=$obj.stroke$line:=New collection$line.push($x)$line.push($y)$line.push($x)$line.push($y+$height)$lineSvg:=SVG_New_line ($svg;$line[0];$line[1];$line[2];$line[3];$lineColor;$stroke)$line[1]:=$y+$height$line[2]:=$x+$width$line[3]:=$line[1]$lineSvg:=SVG_New_line ($svg;$line[0];$line[1];$line[2];$line[3];$lineColor;$stroke): ($obj.type="grad")  //  requires: startColor; endColor; rotation$startColor:=$obj.startColor$endColor:=$obj.endColor$rot:=$obj.rotSVG_Define_linear_gradient ($svg;"g";$startColor;$endColor;$rot;"reflect")$rect:=SVG_New_rect ($svg;$x;$y;$width;$height;$roundedX;$roundedY;$lineColor+":0";"url(#g)")Else End case   // --------------------------------------------------------  // SVGTool_SHOW_IN_VIEWER ($svg)$pic:=SVG_Export_to_picture ($svg;1)$0:=$picSVG_CLEAR ($svg)